apiVersion: v1
kind: Service

metadata:
  name: sre-playground-service
  labels:
    app: sre-playground 
spec:

  selector:
    app: sre-playground

  ports:
  - name: http  
    port: 80
    targetPort: 8000

  type: ClusterIP
