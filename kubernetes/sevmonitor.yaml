apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sre-playground
  namespace: monitoring
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: sre-playground
  namespaceSelector:
    matchNames:
      - default
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
